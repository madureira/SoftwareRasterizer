{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (macOS)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/debug/SoftwareRasterizer",
      "args": [],
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb",
      "preLaunchTask": "cmake: build debug"
    }
  ]
}
